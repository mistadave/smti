# shelly-mqtt-to-influx
shelly mqtt consumer which pushes data to influxdb

This is just a project to tinker around with go, which is used for a local sensor to read out it's data via mqtt and push it's data to influx.
